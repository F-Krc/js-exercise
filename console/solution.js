// write your code in this file
console.log("This is an exercise in console logging.");

const exercise = "This is the value.";
console.log(exercise);

const firstName = "Fatih";
const lastName = "Karaca";
console.log(firstName, lastName);

const city = "Neumünster";

const age = "35";


const job = "web developer";

const profile = firstName + lastName + age + job + city;

console.log(firstName, lastName, age, city, job);

const sentence = profile;


console.log(`${firstName} ${lastName} is a ${age} year old ${job} who lives in ${city}`);
